name: hola mundo

on:
  workflow_dispatch:
    
  # schedule:
  #   - cron: "*/ 5 * * * *"
  # push:
  #   branches:
  #     - main
      

jobs: 
  saludo:
    runs-on: ubuntu-latest
    steps: 
      - name: Muestra mensaje hola mundo
        run: echo "Hola mundo julian"
